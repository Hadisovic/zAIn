export const WIDGET = {
  COLLAPSED_HEIGHT: 60,
  MAX_HEIGHT: 600,
  WIDTH: 400,
  MIN_WIDTH: 320,
} as const;

export const BLOB = {
  SIZE: 90,
  BREATH_PERIOD_MS: 3800,
  HUE_SPEED: 15,
} as const;

export const TIMING = {
  AUTO_COLLAPSE_AFTER_MS: 5000,
  THINKING_GLOW_MS: 1500,
  MESSAGE_FLOAT_MS: 800,
} as const;
